# Task Tracker (Java Console App)

A simple command-line Task Tracker written in Java.

## Features
- Add, view, complete, and delete tasks
- Saves tasks to a local file (`tasks.txt`)
- Lightweight and easy to use

## How to Run
1. Clone the repository:
   ```bash
   git clone https://github.com/YOUR_USERNAME/TaskTracker.git
   cd TaskTracker
